# data_generator.py and build_db.py use the standard library only
# (csv, json, sqlite3, random, uuid, datetime) - nothing to install for
# those.
#
# Needed only to render the PDF documentation via templates/build_pdf.py
# (requires pandoc as a system binary too - see https://pandoc.org/installing.html):
weasyprint>=69.0

# Needed for Assignment 4's statistical analysis and dashboard build:
scipy>=1.11
statsmodels>=0.14
plotly>=5.20

# Optional, only needed if you extend the project (e.g. pandas-based
# exploration notebooks):
pandas>=2.0
